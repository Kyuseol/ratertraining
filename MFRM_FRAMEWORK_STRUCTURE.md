# MFRM 프레임워크 구조

**작성일:** 2025-12-17  
**버전:** Blueprint v0.9 기반 (v0.9.8-alpha)

---

## 목차

1. [개요](#1-개요)
2. [시스템 아키텍처](#2-시스템-아키텍처)
3. [측정 모델](#3-측정-모델)
4. [평가 체계](#4-평가-체계)
5. [데이터베이스 구조](#5-데이터베이스-구조)
6. [API 구조](#6-api-구조)
7. [프론트엔드 구조](#7-프론트엔드-구조)
8. [분석 워크플로](#8-분석-워크플로)
9. [품질 관리 시스템](#9-품질-관리-시스템)
10. [디렉토리 구조](#10-디렉토리-구조)

---

## 1. 개요

### 1.1 프로젝트 목적

MFRM(Many-Facets Rasch Model) 기반 쓰기 평가 문식성 진단 시스템으로, 교사의 채점 역량을 분석하고 개인화된 피드백을 제공합니다.

### 1.2 핵심 기능

| 기능 | 설명 |
|------|------|
| **채점 역량 진단** | MFRM 모델로 평가자 특성 분석 |
| **엄격성 측정** | 개별 교사의 채점 기준 편향 분석 |
| **일관성 분석** | Infit/Outfit 지표로 채점 패턴 일관성 평가 |
| **헤일로 효과 탐지** | 평가요소 간 과도한 상관 감지 |
| **범주 불균형 분석** | 특정 점수 과다 사용 여부 확인 |
| **피드백 제공** | 진단 결과에 기반한 맞춤형 개선 제안 |

### 1.3 대상 사용자

- **교사**: 50-100명 (상시 참여)
- **관리자**: 에세이 관리, 앵커 설정, 분석 실행

---

## 2. 시스템 아키텍처

### 2.1 전체 구조

```
┌─────────────────────────────────────────────────────────────────────┐
│                           Frontend Layer                            │
│                    (React + TypeScript + Netlify)                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────────────┐    │
│  │  Teacher Mode │  │  Admin Mode   │  │   Analysis View       │    │
│  │  - Dashboard  │  │  - Essays     │  │   - MFRM Results      │    │
│  │  - Rating     │  │  - Anchors    │  │   - Reports           │    │
│  │  - Report     │  │  - Dashboard  │  │   - Quality Metrics   │    │
│  │  - Training   │  │               │  │                       │    │
│  └───────────────┘  └───────────────┘  └───────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         Database Layer                              │
│                    (Supabase PostgreSQL)                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────────────┐    │
│  │teachers │ │ essays │ │rubrics │ │ scores │ │ mfrm_results   │    │
│  └─────────┘ └────────┘ └────────┘ └────────┘ └────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        Backend Layer                                │
│                  (R + Plumber + TAM Package)                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────────────┐    │
│  │   fluber.R    │  │   model.R     │  │    utils.R            │    │
│  │   (REST API)  │  │   (MFRM Core) │  │    (Utilities)        │    │
│  └───────────────┘  └───────────────┘  └───────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 데이터 플로우

```
┌──────────┐    1. 채점 데이터 조회    ┌─────────────┐
│ Frontend │ ────────────────────────► │  Supabase   │
└──────────┘                           │ PostgreSQL  │
     │                                 └─────────────┘
     │ 2. 데이터 전송 (HTTP POST)            │
     ▼                                       │
┌──────────┐                                 │
│ R Backend│ ◄───────────────────────────────┘
│ (TAM)    │    3. MFRM 분석 실행
└──────────┘
     │
     │ 4. 분석 결과 반환
     ▼
┌──────────┐    5. 결과 저장          ┌─────────────┐
│ Frontend │ ────────────────────────► │  Supabase   │
└──────────┘                           │ PostgreSQL  │
                                       └─────────────┘
```

### 2.3 기술 스택

| 계층 | 기술 | 버전 |
|------|------|------|
| **Frontend** | React | 18.x |
| | TypeScript | 5.x |
| | @supabase/supabase-js | Latest |
| **Backend** | R | 4.2.3+ |
| | Plumber (REST API) | Latest |
| | TAM (MFRM 모델) | 4.2-21 |
| **Database** | Supabase PostgreSQL | - |
| **Infrastructure** | Netlify (Frontend) | - |
| | Oracle Cloud (Backend) | Always Free |
| | Docker | - |

---

## 3. 측정 모델

### 3.1 MFRM (Many-Facets Rasch Model)

다면 라쉬 모형은 평가자, 피험자, 과제, 평가 척도 등 여러 측면(facets)을 동시에 분석합니다.

```
                    ┌─────────────────────────────────────┐
                    │          MFRM 모델 구조             │
                    └─────────────────────────────────────┘
                                     │
            ┌────────────────────────┼────────────────────────┐
            ▼                        ▼                        ▼
    ┌───────────────┐       ┌───────────────┐       ┌───────────────┐
    │   평가자      │       │    에세이     │       │   평가요소    │
    │   (Rater)     │       │   (Essay)     │       │   (Rubric)    │
    │               │       │               │       │               │
    │ • 엄격성      │       │ • 난이도      │       │ • 범주 경계   │
    │ • 일관성      │       │ • SE         │       │ • 가중치      │
    │ • 헤일로      │       │               │       │               │
    └───────────────┘       └───────────────┘       └───────────────┘
```

### 3.2 핵심 파라미터

| 파라미터 | 설명 | 적정 범위 |
|----------|------|-----------|
| **Severity (엄격성)** | 평가자의 채점 기준 편향 (logit scale) | -0.5 ~ +0.5 |
| **Infit** | 예상 패턴과의 일치도 | 0.7 ~ 1.3 |
| **Outfit** | 극단적 반응에 민감한 일치도 | 0.7 ~ 1.3 |
| **SE (표준오차)** | 추정의 정밀도 | 낮을수록 좋음 |

### 3.3 진단 단계

```
┌─────────────────────────────────────────────────────────────────────┐
│                          진단 워크플로                              │
└─────────────────────────────────────────────────────────────────────┘

   없음         예비 진단        공식 진단        정밀 추적
   (none)     (preliminary)    (official)      (advanced)
     │              │               │               │
     │    6편       │     9편       │     18편      │
     │──────────────┼───────────────┼───────────────┤
     │              │               │               │
     │   관측치     │   관측치      │    관측치     │
     │    54개      │    81개       │    162개      │
     │              │               │               │
     │  SE ≈ 0.40   │  SE ≈ 0.30    │  SE ≈ 0.22    │
     │  ~ 0.50     │  ~ 0.35       │  ~ 0.27       │
     ▼              ▼               ▼               ▼
```

| 단계 | 최소 에세이 | 관측치 | 표준오차 (SE) | 제공 기능 |
|------|------------|--------|--------------|----------|
| **예비 진단** | 6편 | 54 | 0.40~0.50 | 기본 리포트 |
| **공식 진단** | 9편 | 81 | 0.30~0.35 | "안정" 배지 + 정식 리포트 |
| **정밀 추적** | 18편 | 162 | 0.22~0.27 | 시계열 분석 + 드리프트 탐지 |

---

## 4. 평가 체계

### 4.1 평가요소 구조 (8개, 3점 척도)

```
┌─────────────────────────────────────────────────────────────────────┐
│                        평가요소 체계                                │
└─────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────────────────┐
    │                      내용 (3개)                             │
    │  ┌───────────┐  ┌───────────┐  ┌───────────┐               │
    │  │  1. 주장  │  │  2. 이유  │  │  3. 근거  │               │
    │  │           │  │           │  │           │               │
    │  │  명확성   │  │  타당성   │  │  질과 양  │               │
    │  │  일관성   │  │  충분성   │  │           │               │
    │  └───────────┘  └───────────┘  └───────────┘               │
    └─────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────────────────┐
    │                      조직 (3개)                             │
    │  ┌───────────┐  ┌───────────┐  ┌───────────┐               │
    │  │ 4. 통일성 │  │ 5. 응집성 │  │ 6. 완결성 │               │
    │  │           │  │           │  │           │               │
    │  │  주제     │  │  논리적   │  │  서론     │               │
    │  │  중심성   │  │  연결     │  │  본론     │               │
    │  │           │  │           │  │  결론     │               │
    │  └───────────┘  └───────────┘  └───────────┘               │
    └─────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────────────────┐
    │                      표현 (2개)                             │
    │  ┌─────────────────────┐  ┌─────────────────────┐          │
    │  │ 7. 어휘·문장 적절성 │  │ 8. 어문 규범 준수   │          │
    │  │                     │  │                     │          │
    │  │  어휘 선택          │  │  맞춤법             │          │
    │  │  문장 구성          │  │  띄어쓰기           │          │
    │  │                     │  │  문장부호           │          │
    │  └─────────────────────┘  └─────────────────────┘          │
    └─────────────────────────────────────────────────────────────┘
```

### 4.2 채점 기준 (3점 척도)

| 범주 | 평가요소 | 1점 | 2점 | 3점 |
|------|----------|-----|-----|-----|
| **내용** | 주장 | 불명확/일관성 부족 | 일부 부족함 | 명확하고 일관됨 |
| | 이유 | 지지 불충분/양적 부족 | 부분적 지지/다소 부족 | 명확히 지지/충분 |
| | 근거 | 구분 불분명/매우 부족 | 구분되나 다소 부족 | 잘 뒷받침/충분 |
| **조직** | 통일성 | 주제 중심 구성 안됨 | 일부 일관성 없는 내용 | 일관되게 구성 |
| | 응집성 | 연결 부자연스러움 | 일부 흐름 끊김 | 논리적 흐름 유지 |
| | 완결성 | 구조 부족/누락 | 기본 구성 있으나 미흡 | 구조 완성도 있음 |
| **표현** | 어휘·문장 | 오류 잦음/이해 방해 | 일부 부적절/개선 요구 | 적절하고 효과적 |
| | 어문 규범 | 규범 미준수/혼란 | 일부 위반/이해 지장 없음 | 정확하게 지켜짐 |

### 4.3 앵커 에세이 시스템

```
┌─────────────────────────────────────────────────────────────────────┐
│                     앵커 에세이 포트폴리오                          │
└─────────────────────────────────────────────────────────────────────┘

    목적: 신입 교사를 공통척도에 빠르게 탑재
          범주 문턱(1↔2, 2↔3) 안정성 유지

    ┌─────────────────────────────────────────────────────────────┐
    │                                                             │
    │    앵커 에세이: 12~16편 (권장: 16편)                        │
    │    ─────────────────────────────────────                    │
    │    • 각 평가요소에서 경계 사례 최소 2회 노출                │
    │    • 난이도: 저/중/고 균형                                  │
    │    • 경계 근거 주해 포함                                    │
    │                                                             │
    │    캘리브레이션 세트: 24~32편 (권장: 32편)                  │
    │    ─────────────────────────────────────                    │
    │    • 요소별 경계당 ≥60 관측                                │
    │    • 공식: N = ⌈120 / R_panel⌉                             │
    │    • 앵커 8~10편 포함                                       │
    │                                                             │
    └─────────────────────────────────────────────────────────────┘

    앵커 혼입률:
    ┌──────────────┬────────────┐
    │   신규 교사  │    25%     │
    │   유지 단계  │  15~20%    │
    └──────────────┴────────────┘
```

---

## 5. 데이터베이스 구조

### 5.1 ERD (Entity Relationship Diagram)

```
┌─────────────────────────────────────────────────────────────────────┐
│                        Database Schema                              │
└─────────────────────────────────────────────────────────────────────┘

    ┌─────────────┐        ┌─────────────┐        ┌─────────────┐
    │   admins    │        │  teachers   │        │   essays    │
    ├─────────────┤        ├─────────────┤        ├─────────────┤
    │ id (PK)     │        │ id (PK)     │◄──┐    │ id (PK)     │
    │ email       │        │ email       │   │    │ title       │
    │ name        │        │ name        │   │    │ content     │
    │ is_active   │        │ institution │   │    │ is_anchor   │
    └─────────────┘        │ diagnosis   │   │    │ is_calib    │
                           │ _level      │   │    │ difficulty  │
                           └─────────────┘   │    └─────────────┘
                                  │          │           │
                                  │          │           │
                           ┌──────┴──────────┴───────────┴──────┐
                           │                                     │
                           │             scores                  │
                           ├─────────────────────────────────────┤
                           │ id (PK)                             │
                           │ teacher_id (FK) ──► teachers.id     │
                           │ essay_id (FK) ──► essays.id         │
                           │ rubric_id (FK) ──► rubrics.id       │
                           │ score                               │
                           │ rating_duration_seconds             │
                           │ comment                             │
                           └─────────────────────────────────────┘
                                             │
    ┌─────────────┐                          │
    │   rubrics   │◄─────────────────────────┘
    ├─────────────┤
    │ id (PK)     │
    │ category    │
    │ name        │
    │ min_score   │
    │ max_score   │
    │ boundary_*  │
    └─────────────┘


    ┌─────────────────────────────────────────────────────────────┐
    │                    MFRM Analysis Tables                     │
    └─────────────────────────────────────────────────────────────┘

    ┌─────────────┐        ┌─────────────────┐        ┌─────────────────┐
    │  mfrm_runs  │───────►│  mfrm_results   │        │essay_difficulties│
    ├─────────────┤   1:N  ├─────────────────┤        ├─────────────────┤
    │ id (PK)     │        │ id (PK)         │        │ id (PK)         │
    │ name        │        │ run_id (FK)     │        │ run_id (FK)     │
    │ version_id  │        │ teacher_id (FK) │        │ essay_id (FK)   │
    │ is_active   │        │ severity        │        │ difficulty      │
    │ _version    │        │ infit/outfit    │        │ difficulty_se   │
    │ status      │        │ halo_effect     │        │ mean_score      │
    │ median_*    │        │ category_imbal  │        │                 │
    │ separation  │        │                 │        │                 │
    └─────────────┘        └─────────────────┘        └─────────────────┘
```

### 5.2 주요 테이블 명세

#### teachers (교사 정보)
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | Primary Key |
| email | VARCHAR(255) | 이메일 (Unique) |
| name | VARCHAR(100) | 이름 |
| institution | VARCHAR(200) | 소속 기관 |
| essays_rated_count | INTEGER | 채점한 에세이 수 |
| diagnosis_level | VARCHAR(20) | 진단 단계 (none/preliminary/official/advanced) |

#### essays (에세이)
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | Primary Key |
| title | VARCHAR(200) | 제목 |
| content | TEXT | 본문 |
| is_anchor | BOOLEAN | 앵커 에세이 여부 |
| is_calibration | BOOLEAN | 캘리브레이션 세트 포함 여부 |
| anchor_explanation | TEXT | 앵커 해설 카드 |
| difficulty_level | VARCHAR(20) | 난이도 (low/medium/high) |

#### rubrics (평가요소)
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | Primary Key |
| category | VARCHAR(50) | 범주 (내용/조직/표현) |
| name | VARCHAR(100) | 평가요소명 |
| min_score / max_score | INTEGER | 점수 범위 (1~3) |
| boundary_1_2_description | TEXT | 1↔2점 경계 설명 |
| boundary_2_3_description | TEXT | 2↔3점 경계 설명 |

#### scores (채점 데이터)
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | Primary Key |
| teacher_id | UUID | 교사 FK |
| essay_id | UUID | 에세이 FK |
| rubric_id | UUID | 평가요소 FK |
| score | INTEGER | 점수 (1~3) |
| rating_duration_seconds | INTEGER | 채점 소요 시간 |

#### mfrm_results (분석 결과)
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | Primary Key |
| run_id | UUID | 분석 실행 FK |
| teacher_id | UUID | 교사 FK |
| severity | DECIMAL | 엄격성 (logit) |
| severity_se | DECIMAL | 표준오차 |
| infit / outfit | DECIMAL | 일관성 지표 |
| halo_effect_score | DECIMAL | 헤일로 효과 (0~1) |
| category_imbalance_score | DECIMAL | 범주 불균형 (0~1) |

#### expert_scores (전문가 채점 데이터) ✨ NEW
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | Primary Key |
| expert_id | VARCHAR | 전문가 ID (RID) |
| essay_id | UUID | 에세이 FK |
| rubric_id | UUID | 평가요소 FK |
| score | INTEGER | 점수 (1~3) |
| created_at | TIMESTAMP | 생성일시 |

**현재 데이터 현황 (2025-12-17)**:
- 전문가: 5명 (RID: 2, 5, 9, 10, 12)
- 앵커 에세이: 15편 (상/중/하 각 5편)
- 총 채점 데이터: 600건

---

## 6. API 구조

### 6.1 R Backend API (Port 8000)

```
┌─────────────────────────────────────────────────────────────────────┐
│                        R Backend API                                │
│                      (Plumber REST API)                             │
└─────────────────────────────────────────────────────────────────────┘

    /api/mfrm/
    ├── POST /analyze-with-data    ← ✅ 현재 사용 중 (데이터 전달 방식, 2025-12-12)
    │   └── Body: { scores_data: [...] }
    │   └── Response: { teacher_parameters, essay_parameters, ... }
    │   └── Frontend: AnalysisPage.tsx에서 api_v2.ts 사용
    │
    ├── GET /results/:run_id       ← 특정 분석 결과 조회
    │
    ├── GET /teacher/:teacher_id   ← 교사 분석 이력
    │
    ├── GET /active-version        ← 현재 활성 버전 결과
    │
    └── POST /rollback             ← 이전 버전으로 롤백

    /health                        ← 헬스 체크
    └── Response: { status, timestamp, tam_version }
```

### 6.2 API 요청/응답 예시

#### POST /api/mfrm/analyze-with-data

**Request:**
```json
{
  "scores_data": [
    {
      "teacher_id": "uuid-1",
      "essay_id": "uuid-a",
      "rubric_id": "uuid-x",
      "score": 2
    },
    ...
  ]
}
```

**Response:**
```json
{
  "success": true,
  "converged": true,
  "teacher_parameters": [
    {
      "teacher_id": "uuid-1",
      "severity": 0.25,
      "severity_se": 0.15,
      "infit": 0.98,
      "outfit": 1.05,
      "mean_score": 2.1,
      "total_ratings": 72,
      "interpretation": "엄격성: 다소 엄격, 일관성: 일관적",
      "feedback": "양호한 채점 패턴을 보이고 있습니다."
    },
    ...
  ],
  "essay_parameters": [...],
  "fit_statistics": {
    "deviance": 1234.56,
    "aic": 1280.56,
    "bic": 1320.56
  }
}
```

---

## 7. 프론트엔드 구조

### 7.1 페이지 구조

```
┌─────────────────────────────────────────────────────────────────────┐
│                       Frontend Pages                                │
└─────────────────────────────────────────────────────────────────────┘

    교사 모드 (Teacher Mode)
    ────────────────────────
    /teacher
    ├── TeacherDashboard.tsx    ← 진단 단계 + 통계 카드
    │
    /teacher/rating
    ├── RatingPage.tsx          ← 2단 분할 + 아코디언 UI
    │
    /teacher/report
    ├── TeacherReport.tsx       ← MFRM 메트릭 카드
    │
    /teacher/training
    └── TrainingTasks.tsx       ← 7가지 미세 조정 과제

    관리자 모드 (Admin Mode)
    ────────────────────────
    /admin
    ├── AdminDashboard.tsx      ← Blueprint 상태 + 시스템 통계
    │
    /admin/essays
    ├── EssayManagement.tsx     ← 에세이 CRUD
    │
    /admin/anchor
    ├── AnchorManagement.tsx    ← 앵커 포트폴리오
    │
    /admin/analysis
    └── AnalysisPage.tsx        ← MFRM 분석 실행/결과 ✅ api_v2.ts 사용 (2025-12-12)
```

### 7.2 컴포넌트 구조

```
src/
├── components/
│   ├── admin/
│   │   ├── EssayForm.tsx       ← 에세이 입력 폼
│   │   └── EssayForm.css
│   └── RaterTrainingApp.tsx
│
├── contexts/
│   └── AuthContext.tsx         ← 인증 컨텍스트
│
├── lib/
│   ├── api.ts                  ← R Backend API 클라이언트 (구버전, DB 연결 필요)
│   ├── api_v2.ts               ← MFRM 분석 v2 (데이터 전달) ✅ 권장
│   └── supabase.ts             ← Supabase 클라이언트
│
├── pages/
│   ├── LoginPage.tsx
│   ├── TeacherDashboard.tsx + .css
│   ├── RatingPage.tsx + .css
│   ├── TeacherReport.tsx + .css
│   ├── TrainingTasks.tsx + .css
│   ├── AdminDashboard.tsx + .css
│   ├── EssayManagement.tsx
│   ├── AnchorManagement.tsx
│   └── AnalysisPage.tsx        ✅ api_v2.ts 사용 (2025-12-12)
│
└── types/
    └── index.ts                ← TypeScript 타입 정의
```

### 7.3 주요 TypeScript 타입

```typescript
// 교사 인터페이스
interface ITeacher {
  id: string;
  email: string;
  name: string;
  essays_rated_count: number;
  diagnosis_level: 'none' | 'preliminary' | 'official' | 'advanced';
}

// 에세이 인터페이스
interface IEssay {
  id: string;
  title: string;
  content: string;
  is_anchor: boolean;
  is_calibration: boolean;
  anchor_explanation: string | null;
  difficulty_level: 'low' | 'medium' | 'high' | null;
}

// MFRM 결과 인터페이스
interface IMFRMResult {
  teacher_id: string;
  severity: number | null;
  severity_se: number | null;
  infit: number | null;
  outfit: number | null;
  halo_effect_score: number | null;
  category_imbalance_score: number | null;
}
```

---

## 8. 분석 워크플로

### 8.0 캘리브레이션 워크플로 (고정척도 구축) ✨ NEW

```
┌─────────────────────────────────────────────────────────────────────┐
│                  Expert Calibration Workflow                         │
│               (2025-12-17 완료)                                      │
└─────────────────────────────────────────────────────────────────────┘

    Step 1: 전문가 채점 데이터 수집
    ────────────────────────────────
    anchor.csv → 파싱 → expert_scores 테이블
    │
    ├── 전문가 5명 (RID: 2, 5, 9, 10, 12)
    ├── 앵커 에세이 15편 (상/중/하 각 5편)
    ├── 평가요소 8개 (C1-E2)
    └── 총 600건 채점 데이터

    Step 2: 캘리브레이션 실행
    ────────────────────────────────
    Frontend → POST /api/mfrm/analyze-with-data → R Backend
                         │
                         └─► run_expert_calibration()
                              │
                              ├─► TAM::tam.mml() (실패 시)
                              └─► run_simple_expert_calibration() (대체)

    Step 3: 결과 계산
    ────────────────────────────────
    ┌─────────────────────────────────────────────────┐
    │ 전문가 엄격성                                   │
    │ ├── #2: +1.517 logit (가장 엄격)               │
    │ ├── #4: +0.287 logit                           │
    │ ├── #1: -0.123 logit (중립)                    │
    │ ├── #5: -0.533 logit                           │
    │ └── #3: -1.148 logit (가장 관대)               │
    └─────────────────────────────────────────────────┘
    
    ┌─────────────────────────────────────────────────┐
    │ 에세이 난이도                                   │
    │ ├── 상(high):   -3.527 logit (평균)            │
    │ ├── 중(medium): -2.567 logit (평균)            │
    │ └── 하(low):    -1.034 logit (평균)            │
    └─────────────────────────────────────────────────┘

    Step 4: 데이터베이스 저장
    ────────────────────────────────
    ├── mfrm_runs: 캘리브레이션 실행 기록
    ├── essay_difficulties: 15편 난이도 저장
    └── essays: difficulty_logit 업데이트
```

### 8.1 MFRM 분석 플로우

```
┌─────────────────────────────────────────────────────────────────────┐
│                     MFRM Analysis Workflow                          │
└─────────────────────────────────────────────────────────────────────┘

    Step 1: 데이터 수집
    ────────────────────
    Frontend (AnalysisPage.tsx) ──► api_v2.ts ──► Supabase에서 scores 데이터 조회
                 │
                 └─► { teacher_id, essay_id, rubric_id, score }

    Step 2: 데이터 전송
    ────────────────────
    Frontend (api_v2.ts) ──► HTTP POST ──► R Backend (포트 8000)
                               │
                               └─► /api/mfrm/analyze-with-data ✅

    Step 3: TAM 모델 실행
    ────────────────────
    R Backend
    │
    ├── validate_scores_data()     ← 데이터 검증
    │   └── 최소 30개 점수, 3명 교사, 10편 에세이
    │
    ├── prepare_data_for_tam()     ← Wide format 변환
    │   └── Response matrix 생성
    │
    ├── TAM::tam.mml.mfr()         ← MFRM 모델 피팅
    │   └── formulaA = ~ item + rater + step
    │
    ├── extract_teacher_parameters() ← 교사 파라미터 추출
    │   └── severity, infit, outfit
    │
    └── interpret_mfrm_results()   ← 해석 및 피드백 생성

    Step 4: 결과 저장
    ────────────────────
    Frontend (api_v2.ts) ──► Supabase
    │
    ├── mfrm_runs 테이블에 실행 기록 ✅
    ├── mfrm_results 테이블에 교사별 결과 ✅
    └── essay_difficulties 테이블에 에세이 난이도 ✅
    
    ✅ 전체 플로우 검증 완료 (2025-12-12)
```

### 8.2 R 모델 핵심 함수

```r
# 메인 분석 함수
fit_mfrm <- function(scores_data) {
  # 데이터 검증
  validate_scores_data(scores_data)
  
  # TAM 형식으로 변환
  prepared_data <- prepare_data_for_tam(scores_data)
  
  # MFRM 모델 피팅
  model <- TAM::tam.mml.mfr(
    resp = prepared_data$response_matrix,
    facets = prepared_data$facets,
    formulaA = ~ item + rater + step
  )
  
  # 파라미터 추출
  teacher_params <- extract_teacher_parameters(model, prepared_data)
  essay_params <- extract_essay_parameters(model, prepared_data)
  
  return(list(
    model = model,
    converged = model$converged,
    teacher_parameters = teacher_params,
    essay_parameters = essay_params
  ))
}
```

---

## 9. 품질 관리 시스템

### 9.1 배치 재추정

```
┌─────────────────────────────────────────────────────────────────────┐
│                     Batch Re-estimation                             │
└─────────────────────────────────────────────────────────────────────┘

    주기:
    ├── 파일럿: 2주 1회
    └── 본운영: 주 1회

    수용 임계:
    ┌─────────────────────────────────────────────────┐
    │ 지표                │ 수용 기준                 │
    ├─────────────────────┼───────────────────────────┤
    │ 인핏 중앙값         │ 0.7 ~ 1.3                 │
    │ 아웃핏 중앙값       │ 0.7 ~ 1.3                 │
    │ 분리지수            │ ≥ 1.5                     │
    │ 극단 응답률         │ ≤ 10%                     │
    │ 요소별 문턱 순서    │ 정상 (1 < 2 < 3)          │
    └─────────────────────┴───────────────────────────┘

    미충족 시:
    └── 이전 버전 유지 + 개선 리포트 배포
```

### 9.2 버전 관리

```
┌─────────────────────────────────────────────────────────────────────┐
│                      Version Control                                │
└─────────────────────────────────────────────────────────────────────┘

    mfrm_runs 테이블:
    ├── version_id: "v1.0.0", "v1.0.1", ...
    └── is_active_version: 현재 활성 버전

    롤백 프로세스:
    ┌──────────────┐     품질 기준     ┌──────────────┐
    │  v1.0.2      │ ───미충족────► │  v1.0.1로    │
    │  (신규)      │                │  자동 롤백   │
    └──────────────┘                └──────────────┘
```

### 9.3 추가 진단 지표

#### 헤일로 효과 (Halo Effect)
```
평가요소 간 상관계수 분석
├── 계산: 교사별 루브릭 간 평균 상관
├── 기준: > 0.7 (의심), > 0.9 (문제)
└── 정규화: 0~1 (높을수록 문제)
```

#### 범주 불균형 (Category Imbalance)
```
점수 분포 분석
├── 이상적 분포: 1점(25%), 2점(50%), 3점(25%)
├── 계산: Chi-square 거리
└── 정규화: 0~1 (높을수록 문제)
```

---

## 10. 디렉토리 구조

```
mfrm-project/
│
├── backend/                      # R 백엔드
│   ├── fluber.R                  # Plumber API 엔드포인트
│   ├── model.R                   # MFRM 모델 구현 (TAM)
│   ├── utils.R                   # 유틸리티 함수
│   ├── db.R                      # 데이터베이스 연동 (현재 미사용)
│   ├── analyze_with_data.R       # 데이터 전달 방식 MFRM 분석
│   ├── simple_analysis.R         # 간단한 분석 (테스트용)
│   ├── start_api.ps1             # Windows 실행 스크립트
│   ├── start_api.bat
│   ├── Dockerfile                # R 컨테이너 설정
│   └── docker-compose.yml
│
├── frontend/                     # React 프론트엔드
│   ├── public/
│   │   └── index.html
│   ├── src/
│   │   ├── App.tsx               # 메인 앱 컴포넌트
│   │   ├── index.tsx             # 엔트리 포인트
│   │   ├── index.css             # 전역 스타일
│   │   ├── components/
│   │   │   ├── admin/
│   │   │   │   ├── EssayForm.tsx
│   │   │   │   └── EssayForm.css
│   │   │   └── RaterTrainingApp.tsx
│   │   ├── contexts/
│   │   │   └── AuthContext.tsx   # 인증 컨텍스트
│   │   ├── lib/
│   │   │   ├── api.ts            # R API 클라이언트
│   │   │   ├── api_v2.ts         # MFRM v2 API
│   │   │   └── supabase.ts       # Supabase 클라이언트
│   │   ├── pages/
│   │   │   ├── LoginPage.tsx
│   │   │   ├── TeacherDashboard.tsx + .css
│   │   │   ├── RatingPage.tsx + .css
│   │   │   ├── TeacherReport.tsx + .css
│   │   │   ├── TrainingTasks.tsx + .css
│   │   │   ├── AdminDashboard.tsx + .css
│   │   │   ├── EssayManagement.tsx
│   │   │   ├── AnchorManagement.tsx
│   │   │   └── AnalysisPage.tsx
│   │   ├── types/
│   │   │   └── index.ts          # TypeScript 타입
│   │   └── utils/
│   │       └── helpers.ts
│   ├── package.json
│   ├── tsconfig.json
│   └── yarn.lock
│
├── database/                     # 데이터베이스
│   ├── schema.sql                # Supabase 테이블 스키마
│   ├── update_rubrics.sql        # 루브릭 업데이트 SQL
│   ├── add_sample_data.sql       # 샘플 데이터 추가
│   ├── add_sample_data.js        # 샘플 데이터 JS
│   ├── fix_rls_policies.sql      # RLS 정책 수정
│   └── disable_rls_for_testing.sql
│
├── docs/                         # 문서
│   ├── ORACLE_SETUP.md           # Oracle Cloud 설정
│   └── SUPABASE_SETUP.md         # Supabase 설정
│
├── README.md                     # 프로젝트 README
├── CLAUDE.md                     # 프로젝트 컨텍스트
├── blueprint.md                  # Blueprint v0.9 설계 문서
├── HOW_TO_RUN.md                 # 실행 가이드
├── PILOT_TEST_GUIDE.md           # 파일럿 테스트 가이드
├── QUICK_START.md                # 빠른 시작
└── MFRM_FRAMEWORK_STRUCTURE.md   # ← 현재 문서
```

---

## 부록

### A. 공식 및 계산식

| 공식 | 설명 |
|------|------|
| `N_cal = ⌈120 / R_panel⌉` | 캘리브레이션 필요 에세이 수 |
| `관측치 = 에세이 수 × E` | 총 관측치 계산 (E=8) |
| `분리지수 = √((σ²_severity - σ²_se) / σ²_se)` | 평가자 분리 지표 |

### B. 환경 변수

**Backend (.env)**
```bash
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_KEY=eyJxxx...
API_PORT=8000
API_HOST=0.0.0.0
```

**Frontend (.env)**
```bash
REACT_APP_SUPABASE_URL=https://xxx.supabase.co
REACT_APP_SUPABASE_ANON_KEY=eyJxxx...
REACT_APP_R_API_URL=http://localhost:8000
```

### C. 참고 자료

- **TAM Package**: [CRAN - TAM](https://cran.r-project.org/package=TAM)
- **Many-Facets Rasch Model**: Linacre, J. M. (1989)
- **Supabase Documentation**: [supabase.com/docs](https://supabase.com/docs)

---

**Last Updated:** 2025-12-17  
**Version:** v0.9.8-alpha  
**Status:** 전문가 캘리브레이션 완료, 고정척도 구축 완료 🎊

### 최근 업데이트 (2025-12-17)

#### 전문가 캘리브레이션 완료 ✅ NEW

Blueprint v0.9의 고정척도 기반 시스템 구현을 위한 전문가 캘리브레이션이 성공적으로 완료되었습니다.

**캘리브레이션 결과**:
- 전문가 5명의 엄격성 파라미터 추정 완료 (-1.148 ~ +1.517 logit)
- 앵커 에세이 15편의 난이도 계산 완료 (-4.369 ~ -0.619 logit)
- 데이터베이스 저장 완료 (mfrm_runs, essay_difficulties, essays 테이블)

**난이도별 통계**:
| 난이도 | 에세이 수 | 평균 logit | 평균 점수 |
|--------|-----------|------------|-----------|
| 상(high) | 5편 | -3.527 | 2.93 |
| 중(medium) | 5편 | -2.567 | 2.85 |
| 하(low) | 5편 | -1.034 | 2.46 |

**기술적 변경**:
- `backend/fluber.R`: `run_expert_calibration()` 호출 연동
- `backend/analyze_with_data.R`: 버그 수정 (서브셋팅, 오류 처리)
- 분석 방법: Simple Expert MFRM (TAM sfsmisc 패키지 부재로 대체)

---

### 이전 업데이트 (2025-12-12)

#### 백엔드/프론트엔드 연동 완료 ✅
- ✅ **AnalysisPage.tsx 업데이트**: `api_v2.ts` 사용 (데이터 전달 방식)
- ✅ **백엔드 실행 확인**: R 4.2.3, 포트 8000 정상 작동
- ✅ **API 엔드포인트 검증**: 15개 엔드포인트 모두 정상 작동
- ✅ **문서화**: `BACKEND_FRONTEND_ANALYSIS.md` 생성

#### 주요 변경사항
- `frontend/src/pages/AnalysisPage.tsx`: 구버전 API (`api.ts`) → 신버전 API (`api_v2.ts`)로 변경
- `fetchRuns()`, `viewResults()`: Supabase 직접 조회로 변경 (DB 연결 문제 우회)
- 백엔드 헬스 체크 및 모든 엔드포인트 정상 작동 확인

